@model IEnumerable<BAPA_LMS.Models.CourseViewModels.CourseListViewModel>

@{
    ViewBag.Title = "Kurser";
}
@section scripts{
    <script src="~/Scripts/jquery.unobtrusive-ajax.min.js"></script>
    <script src="~/Scripts/filter.js"></script>
}

@using (Ajax.BeginForm(
                                new AjaxOptions
                                {
                                    HttpMethod = "get",
                                    InsertionMode = InsertionMode.Replace,
                                    UpdateTargetId = "vehicleList"
                                }))
{
    <label for="vehiclefilter" class="control-label">Filtrera: </label>
    <input id="vehiclefilter" type="search" name="filter" class="form-control" />
    <p></p>
    @Html.Partial("_TeacherIndex", Model)
}
        
    <div>
    @Html.ActionLink("Lägg till lärare", "RegisterTeacher")
        @Html.ActionLink("Lägg till kurs", "Create","Courses")
</div>