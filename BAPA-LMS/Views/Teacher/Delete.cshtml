@model BAPA_LMS.Models.UserViewModels.DeleteUserViewModel

@{
    ViewBag.Title = "DeleteConfirmed";
}



<h3>Är du säker att du vill ta bort användaren?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.CourseName)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.CourseName)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.FullName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.FullName)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Email)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Email)
        </dd>    
    </dl>

    @using (Html.BeginForm("DeleteConfirmed","Teacher", new { id = Model.Id })) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Tillbaka", "CourseEdit", new { id = Model.CourseId } )
        </div>
    }
</div>
